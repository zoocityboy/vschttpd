name: vschttpd
description: |
  Local development server with support for running
  flutter web application in vscode webview extensions

version: 1.0.0
environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  args: ^2.3.0
  shelf: ^1.2.0
  shelf_static: ^1.1.0
  path: ^1.8.0

dev_dependencies:
  test: ^1.19.3
executables:
  vschttpd:
